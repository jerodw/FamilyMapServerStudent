package Service;
import Model.*;
import java.util.*;
import DAO.*;

/**
 * class for handling the person service
 */
public class PersonService {

    /**
     * searches the person DAO for the person object with the specified person ID who also belongs to the current user if personID is null, then returns vector of all persons owned by the current user
     * @param personID the identifier for the person object.
     */
    public PersonService(String personID) {}

    public Person getPerson() {return null;}

    public Vector<Person> getPersons() {return null;}
}
