package Service;
import DAO.*;
import Request.LoadRequest;

/**
 * class to handle the load service
 */
public class LoadService {

    /**
     * constructor for class. calls the clear class to erase the database, then loads the arrays into each of their respective classes
     * @param request the LoadRequest object that holds the data from the json string
     */
    public LoadService(LoadRequest request) {}
}
